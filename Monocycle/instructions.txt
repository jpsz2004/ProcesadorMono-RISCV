/* Para el siguiente código */
#include <stdio.h>

int factorial(){
    int n = 10;
    if(n <= 1){
        return 1;
    }

    int result = 1;

    while (n > 1){
        result *= n;
        n--;
    }

    return result;
}

/* Las instrucciones generadas son: */
factorial():
        addi    sp,sp,-32
        sw      ra,28(sp)
        sw      s0,24(sp)
        addi    s0,sp,32
        li      a5,10
        sw      a5,-20(s0)
        lw      a4,-20(s0)
        li      a5,1
        bgt     a4,a5,.L2
        li      a5,1
        j       .L3
.L2:
        li      a5,1
        sw      a5,-24(s0)
        j       .L4
.L5:
        lw      a4,-24(s0)
        lw      a5,-20(s0)
        mul     a5,a4,a5
        sw      a5,-24(s0)
        lw      a5,-20(s0)
        addi    a5,a5,-1
        sw      a5,-20(s0)
.L4:
        lw      a4,-20(s0)
        li      a5,1
        bgt     a4,a5,.L5
        lw      a5,-24(s0)
.L3:
        mv      a0,a5
        lw      ra,28(sp)
        lw      s0,24(sp)
        addi    sp,sp,32
        jr      ra
